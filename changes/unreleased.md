# Unreleased

## Added

- Causal (expanding-window) scaling mode for `LogReturnProcessor`: set `scaling: expanding` in data config to z-score returns without look-ahead bias. Configurable `min_periods` burn-in (default 60).

## Changed

## Fixed

## Removed
